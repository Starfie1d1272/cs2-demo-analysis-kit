{
  "name": "event-upload-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker：赛事包上传隔离区（submission quarantine）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
