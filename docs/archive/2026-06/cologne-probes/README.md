# Cologne Tactical Probes

这些脚本是 2026-06 Coach / site-entry / 默认位语义调参时的一次性探针。
它们保留为研究证据，不属于当前维护脚本入口；通用赛事制作入口见
`scripts/hltv/`、`scripts/event-export.mjs`、`scripts/build-event-package.mjs`。

原始 Cologne event spec 与 HLTV URL/BP 文本已移动到
`fixtures/events/cologne-major-2026/`，作为可复用 fixture provenance。
